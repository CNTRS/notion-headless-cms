## ADDED Requirements

_(none — this change is a pure dependency and adapter upgrade. No new domain-level requirements.)_

## MODIFIED Requirements

_(none — the `IPageRepository` and `IImageFetcher` interfaces and all domain types remain unchanged.)_
